kind: Service
apiVersion: v1
metadata:
  name: helidon-mongodb-cqrs
  labels:
    app: helidon-mongodb-cqrs
spec:
  type: ClusterIP
  selector:
    app: helidon-mongodb-cqrs
  ports:
    - name: tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: helidon-mongodb-cqrs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helidon-mongodb-cqrs
  template:
    metadata:
      labels:
        app: helidon-mongodb-cqrs
        version: v1
    spec:
      containers:
      - name: helidon-mongodb-cqrs
        image: helidon-mongodb-cqrs
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
